steps:
  - label: ":go: build"
    command: 
      - goreleaser build --rm-dist --skip-validate
      - buildkite-agent artifact upload "/workdir/dist/*"
    expeditor:
      executor:
        docker:

  - wait

  - label: ":artifactory: upload"
    command: 
      - buildkite-agent artifact download "/workdir/dist/*" .
      - .expeditor/artifact_upload.sh
    expeditor:
      secrets:
        ARTIFACTORY_TOKEN:
          path: account/static/artifactory/buildkite
          field: token
      executor:
        docker:
